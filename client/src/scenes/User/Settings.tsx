import { Box } from '@mui/material'
import React from 'react'
import Header from '../../components/Header'

const Settings = () => {
  return (
<Box m={'20px'}>
    <Header title={'Titre'} subtitle={'Sous-titres'} addlink={''} withbtn={true} variant={''}></Header>
</Box>
  )
}

export default Settings
